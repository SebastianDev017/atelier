{%- comment -%}
  Newsletter signup. Centered editorial form on a narrow column.
  Mirrors rich-text.liquid: inline <style> carries instance values only,
  a color-scheme--* wrapper paints the background, {% stylesheet %} holds
  all component CSS with no Liquid, and data-animate drives global reveal.
{%- endcomment -%}
{%- liquid
  assign scheme = section.settings.color_scheme.id | default: 'inverse'
  assign placeholder = section.settings.input_placeholder | escape
  assign button_label = section.settings.button_label | escape
  assign heading_tag = section.settings.heading_tag | default: 'h2'
  assign heading_size = section.settings.heading_size | default: 'lg'
-%}

<style>
  #shopify-section-{{ section.id }} {
    --pt: {{ section.settings.padding_top }}px;
    --pb: {{ section.settings.padding_bottom }}px;
  }
</style>

<div class="newsletter color-scheme--{{ scheme }}">
  <span class="edition-number" aria-hidden="true">006</span>
  <div class="newsletter__inner page-width page-width--narrow">
    {%- if section.settings.heading != blank -%}
      <{{ heading_tag }} class="newsletter__heading section-heading heading--{{ heading_size }}" data-animate>
        {{- section.settings.heading | escape -}}
      </{{ heading_tag }}>
    {%- endif -%}

    {%- if section.settings.subtext != blank -%}
      <div class="newsletter__subtext prose" data-animate data-animate-delay="2">
        {{ section.settings.subtext }}
      </div>
    {%- endif -%}

    {%- comment -%}
      An explicit id: Shopify gives every `customer` form the default id
      "contact_form", so this form and the footer's shipped with the SAME id on
      every page -- invalid HTML, and an ambiguous target for anything that looks
      a form up by id (B2).
    {%- endcomment -%}
    {%- assign newsletter_form_id = 'NewsletterForm-' | append: section.id -%}
    {%- form 'customer', id: newsletter_form_id, class: 'newsletter__form', data-animate: true, data-animate-delay: '3' -%}
      <input type="hidden" name="contact[tags]" value="newsletter">

      {%- if form.posted_successfully? -%}
        <p class="form-status form-status--success" role="status">
          {{- 'newsletter.success' | t -}}
        </p>
      {%- else -%}
        <div class="newsletter__row">
          <label class="visually-hidden" for="NewsletterEmail-{{ section.id }}">
            {{- 'newsletter.label' | t -}}
          </label>
          <input
            type="email"
            name="contact[email]"
            id="NewsletterEmail-{{ section.id }}"
            class="input newsletter__input"
            placeholder="{{ placeholder }}"
            autocomplete="email"
            required
          >
          <button type="submit" class="btn newsletter__button">
            {{- button_label -}}
          </button>
        </div>

        {%- if form.errors -%}
          {%- comment -%} A <div>, not a <p>: default_errors emits block markup (div.errors > ul > li), and a <p> cannot contain it -- the parser closes the <p> first, leaving this role="alert" box EMPTY and the message outside it, unstyled (B6). {%- endcomment -%}
          <div class="form-status form-status--error" role="alert">
            {{- form.errors | default_errors -}}
          </div>
        {%- endif -%}
      {%- endif -%}
    {%- endform -%}
  </div>
</div>

{% stylesheet %}
  .newsletter { position: relative; padding-block: var(--pt, var(--section-spacing)) var(--pb, var(--section-spacing)); }
  .newsletter__inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-5);
    text-align: center;
  }
  .newsletter__heading { max-width: 24ch; }
  .newsletter__subtext { max-width: 52ch; color: var(--color-muted); }
  .newsletter__form { width: 100%; }
  .newsletter__row {
    display: flex;
    flex-direction: column;
    gap: var(--space-3);
    width: 100%;
    max-width: 32rem;
    margin-inline: auto;
    text-align: left;
  }
  .newsletter__input { flex: 1 1 auto; }
  .newsletter__button { flex: 0 0 auto; }
  .newsletter .form-status { text-align: center; }

  @media (min-width: 750px) {
    .newsletter__row {
      flex-direction: row;
      align-items: stretch;
    }
    .newsletter__button { width: auto; }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:sections.newsletter.name",
  "tag": "section",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "t:sections.newsletter.settings.heading.label",
      "default": "Join the list"
    },
    {
      "type": "select",
      "id": "heading_tag",
      "label": "t:sections.newsletter.settings.heading_tag.label",
      "info": "t:sections.newsletter.settings.heading_tag.info",
      "options": [
        { "value": "h1", "label": "t:sections.newsletter.settings.heading_tag.options__1.label" },
        { "value": "h2", "label": "t:sections.newsletter.settings.heading_tag.options__2.label" },
        { "value": "h3", "label": "t:sections.newsletter.settings.heading_tag.options__3.label" },
        { "value": "h4", "label": "t:sections.newsletter.settings.heading_tag.options__4.label" },
        { "value": "p", "label": "t:sections.newsletter.settings.heading_tag.options__5.label" }
      ],
      "default": "h2"
    },
    {
      "type": "select",
      "id": "heading_size",
      "label": "t:sections.newsletter.settings.heading_size.label",
      "info": "t:sections.newsletter.settings.heading_size.info",
      "options": [
        { "value": "display", "label": "t:sections.newsletter.settings.heading_size.options__1.label" },
        { "value": "xl", "label": "t:sections.newsletter.settings.heading_size.options__2.label" },
        { "value": "lg", "label": "t:sections.newsletter.settings.heading_size.options__3.label" },
        { "value": "md", "label": "t:sections.newsletter.settings.heading_size.options__4.label" },
        { "value": "sm", "label": "t:sections.newsletter.settings.heading_size.options__5.label" },
        { "value": "xs", "label": "t:sections.newsletter.settings.heading_size.options__6.label" },
        { "value": "body", "label": "t:sections.newsletter.settings.heading_size.options__7.label" }
      ],
      "default": "lg"
    },
    {
      "type": "richtext",
      "id": "subtext",
      "label": "t:sections.newsletter.settings.subtext.label",
      "default": "<p>Quiet updates, new arrivals, and the occasional note. No noise.</p>"
    },
    {
      "type": "text",
      "id": "input_placeholder",
      "label": "t:sections.newsletter.settings.input_placeholder.label",
      "default": "Email address"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "t:sections.newsletter.settings.button_label.label",
      "default": "Subscribe"
    },
    {"type": "color_scheme", "id": "color_scheme", "label": "t:sections.newsletter.settings.color_scheme.label", "default": "inverse"},
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "t:sections.newsletter.settings.padding_top.label",
      "default": 96
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 160,
      "step": 4,
      "unit": "px",
      "label": "t:sections.newsletter.settings.padding_bottom.label",
      "default": 96
    }
  ],
  "presets": [
    { "name": "t:sections.newsletter.presets.name" }
  ]
}
{% endschema %}
