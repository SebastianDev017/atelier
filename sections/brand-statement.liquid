{%- comment -%}
  Brand statement — full-viewport pinned section, pure display type. As the user
  scrolls through, the heading reveals word-by-word (opacity 0.1 → 1) via a
  pinned, scrubbed GSAP timeline (initBrandStatement in gsap-animations.js).
  data-no-split keeps the generic SplitText (h2 target) off this heading.
{%- endcomment -%}
{%- liquid
  assign scheme = section.settings.color_scheme | default: 'cream'
  assign heading = section.settings.heading | default: 'Considered objects for the modern home'
-%}

<div class="brand-statement section--full-bleed color-scheme--{{ scheme }}" data-brand-statement>
  <div class="brand-statement__inner page-width">
    <h2 class="brand-statement__heading" data-no-split>{{ heading | escape | newline_to_br }}</h2>
    {%- if section.settings.subheading != blank -%}
      <p class="brand-statement__sub">{{ section.settings.subheading | escape }}</p>
    {%- endif -%}
  </div>
</div>

<style>
  #shopify-section-{{ section.id }} {
    --bs-size: {{ section.settings.heading_size_vw | default: 10 }}vw;
    --bs-size-mobile: {{ section.settings.heading_size_mobile | default: 13 }}vw;
  }
</style>

{% stylesheet %}
  .brand-statement {
    min-height: 100dvh;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    overflow: hidden;
  }
  .brand-statement__inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: clamp(2rem, 5vw, 4rem);
  }
  .brand-statement__heading {
    font-family: var(--font-heading);
    font-size: var(--bs-size, 10vw);
    font-weight: 300;
    line-height: 1;
    letter-spacing: -0.03em;
    max-width: 16ch;
    margin: 0;
  }
  @media (max-width: 767px) {
    .brand-statement__heading { font-size: var(--bs-size-mobile, 13vw); }
  }
  .brand-statement__sub {
    font-size: var(--text-md);
    color: var(--color-muted);
    max-width: 44ch;
    margin: 0;
  }
  .brand-statement .bs-word { display: inline-block; }
{% endstylesheet %}

{% schema %}
{
  "name": "t:sections.brand-statement.name",
  "tag": "section",
  "class": "shopify-section",
  "settings": [
    { "type": "textarea", "id": "heading", "label": "t:sections.brand-statement.settings.heading.label", "default": "Considered objects\nfor the modern home", "info": "t:sections.brand-statement.settings.heading.info" },
    { "type": "text", "id": "subheading", "label": "t:sections.brand-statement.settings.subheading.label", "default": "Made in small batches, designed to last a lifetime." },
    { "type": "range", "id": "heading_size_vw", "label": "t:sections.brand-statement.settings.heading_size_vw.label", "min": 5, "max": 16, "step": 1, "default": 10, "unit": "vw" },
    { "type": "range", "id": "heading_size_mobile", "label": "t:sections.brand-statement.settings.heading_size_mobile.label", "min": 8, "max": 20, "step": 1, "default": 13, "unit": "vw" },
    {
      "type": "select", "id": "color_scheme", "label": "t:sections.brand-statement.settings.color_scheme.label",
      "options": [
        { "value": "cream", "label": "t:sections.brand-statement.settings.color_scheme.options__1.label" },
        { "value": "parchment", "label": "t:sections.brand-statement.settings.color_scheme.options__2.label" },
        { "value": "linen", "label": "t:sections.brand-statement.settings.color_scheme.options__3.label" },
        { "value": "white", "label": "t:sections.brand-statement.settings.color_scheme.options__4.label" },
        { "value": "invert", "label": "t:sections.brand-statement.settings.color_scheme.options__5.label" }
      ], "default": "invert"
    }
  ],
  "presets": [ { "name": "t:sections.brand-statement.presets.name" } ]
}
{% endschema %}
